name: Desktop

on:
  pull_request:
    # Emit the required aggregate for protected main PRs. Job-level guards
    # reserve long desktop audits for main and explicit dispatches.
    branches: [main]
    types: [ready_for_review]
  # Explicit manual final audit path: dispatch this workflow to force the
  # Linux release compilation on any branch, independent of PR state.
  workflow_dispatch:

permissions:
  contents: read

# All Linux desktop checks compile the same host-targeted sidecar. Keep the
# target explicit so cached artifacts cannot cross a future target change.
env:
  CORTANA_DESKTOP_TARGET: x86_64-unknown-linux-gnu

concurrency:
  group: desktop-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Keep the required aggregate check present on every main/release PR
  # without starting the expensive desktop jobs for backend-only changes.
  changes:
    name: Detect desktop changes
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-slim
    timeout-minutes: 5
    outputs:
      desktop: ${{ steps.detect.outputs.desktop }}
    steps:
      - name: Checkout PR
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Detect desktop paths
        id: detect
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            echo "desktop=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          is_desktop_path() {
            case "$1" in
              .github/workflows/desktop.yml|.github/workflows/desktop-acceptance.yml|bun.lock|package.json|Cargo.toml|Cargo.lock|rust-toolchain.toml|CHANGELOG.md|pyproject.toml|uv.lock|README.md|LICENSE|scripts/prepare-desktop-resources.mjs|scripts/prepare-desktop-sidecar.mjs|scripts/acceptance-provenance.mjs|scripts/acceptance-provenance.test.mjs|scripts/backup-restore-drill.sh|scripts/desktop-package-acceptance.mjs|scripts/desktop-package-acceptance.test.mjs|scripts/desktop-install-acceptance.mjs|scripts/desktop-install-acceptance.test.mjs|scripts/desktop-host-launch.mjs|scripts/desktop-host-launch.test.mjs|scripts/desktop-control-plane-acceptance.mjs|scripts/desktop-control-plane-acceptance.test.mjs|scripts/desktop-service-status-acceptance.mjs|scripts/desktop-service-status-acceptance.test.mjs|scripts/desktop-source-authorization-acceptance.mjs|scripts/desktop-source-authorization-acceptance.test.mjs|scripts/desktop-macos-lifecycle-acceptance.mjs|scripts/desktop-macos-lifecycle-acceptance.test.mjs|scripts/desktop-acceptance-matrix.mjs|scripts/desktop-acceptance-matrix.test.mjs|scripts/desktop-cache.test.mjs|scripts/knowledge-accessibility-acceptance.mjs|scripts/knowledge-accessibility-browser.mjs|scripts/knowledge-accessibility-browser.test.mjs|scripts/knowledge-accessibility-matrix.mjs|scripts/knowledge-accessibility-matrix.test.mjs|scripts/static-web-server.mjs|scripts/static-web-server.test.mjs|scripts/verify-desktop-acceptance-evidence.py|scripts/verify-vendored-glib.mjs|scripts/verify-macos-package.sh|scripts/verify-macos-trust.sh)
                return 0
                ;;
            esac

            # Use an explicit prefix test instead of pathname-style globs. This
            # covers nested Rust/Python/UI files (for example src/cortana/main.rs)
            # while keeping unrelated repository paths out of the final audit.
            local prefix
            for prefix in apps/desktop/ apps/web/ src/ eval/ third_party/glib-0.18.5/; do
              if [[ "$1" == "$prefix"* ]]; then
                return 0
              fi
            done
            return 1
          }

          changed=false
          while IFS= read -r path; do
            if is_desktop_path "$path"; then
              changed=true
              break
            fi
          done < <(git diff --name-only "$BASE_SHA" "$HEAD_SHA")
          echo "desktop=$changed" >> "$GITHUB_OUTPUT"

  # Compiler caches have one owner per job. An immutable shared key could be
  # reserved by a fast metadata job before release compilation finishes, leaving
  # later runs without the expensive outputs. Keep fallback keys within the same
  # job/platform/toolchain and cache only the directories that job actually uses.
  # The root target directory belongs to the sidecar, not the Tauri workspace.
  # No per-commit keys or extra warming builds: existing manual main audits can
  # warm the default-branch cache; PR caches retain GitHub's merge-ref isolation.
  # The v2 namespace intentionally starts cold. Compare a cold and a warm run,
  # including cache transfer/save time, before claiming a wall-clock reduction.
  # GTK provenance checks are independent from the iterator test, so run
  # them in parallel to avoid one expensive Rust test blocking the other.
  gtk_provenance:
    name: GTK Provenance
    needs: changes
    # Desktop-specific jobs are final-audit work; keep these jobs for main PRs
    # and explicit dispatches only.
    if: >-
      needs.changes.outputs.desktop == 'true' && (
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.base.ref == 'main' &&
       !startsWith(github.event.pull_request.head.ref, 'release-please--branches--main')))
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf
      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.0
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Verify patched GTK dependency provenance
        run: bun scripts/verify-vendored-glib.mjs

  # Keep the patched GTK iterator test independent from provenance checks so a
  # single failing check does not hide unrelated status in the other.
  gtk_iterator:
    name: GTK iterator (release)
    needs: changes
    # Desktop-specific jobs are final-audit work; keep these jobs for main PRs
    # and explicit dispatches only.
    if: >-
      needs.changes.outputs.desktop == 'true' && (
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.base.ref == 'main' &&
       !startsWith(github.event.pull_request.head.ref, 'release-please--branches--main')))
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf
      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.0
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Cache Rust build artifacts
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            third_party/glib-0.18.5/target
          key: >-
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-${{ hashFiles('Cargo.toml', 'Cargo.lock', 'apps/desktop/src-tauri/Cargo.toml', 'third_party/glib-0.18.5/Cargo.lock') }}-${{ hashFiles('apps/desktop/src-tauri/Cargo.lock') }}-${{ hashFiles('third_party/glib-0.18.5/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-
      - name: Test patched GTK iterator in release mode
        run: cargo test --manifest-path third_party/glib-0.18.5/Cargo.toml --release --lib variant_iter

  security_audit:
    name: Security Audit (cargo-audit)
    needs: changes
    # Keep the dependency scan independent from the provenance and build
    # setup. It needs only the lockfile and the pinned cargo-audit binary;
    # the registry cache accelerates dependency downloads on a cold binary miss.
    if: >-
      needs.changes.outputs.desktop == 'true' && (
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.base.ref == 'main' &&
       !startsWith(github.event.pull_request.head.ref, 'release-please--branches--main')))
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # Compiling cargo-audit from source on a cold binary-cache miss needs
      # dependency downloads only; this job never consumes desktop target trees.
      - name: Cache Rust build artifacts
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: >-
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-${{ hashFiles('Cargo.toml', 'Cargo.lock', 'apps/desktop/src-tauri/Cargo.toml', 'third_party/glib-0.18.5/Cargo.lock') }}-${{ hashFiles('apps/desktop/src-tauri/Cargo.lock') }}-${{ hashFiles('third_party/glib-0.18.5/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-
      # Warm the exact pinned binary under a stable per-OS/arch key so
      # repeated final audits restore cargo-audit without recompiling it.
      - name: Cache cargo-audit binary
        id: cache-cargo-audit
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ~/.cargo/bin/cargo-audit
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-audit-0.22.2
      - name: Install cargo-audit
        if: steps.cache-cargo-audit.outputs.cache-hit != 'true'
        run: cargo install cargo-audit --version 0.22.2 --locked
      - name: Audit desktop Rust dependencies
        run: cargo audit --file apps/desktop/src-tauri/Cargo.lock

  # Keep desktop tests and clippy in separate jobs so a lint failure does not
  # delay test-only feedback, and a test-only fail does not block clippy output.
  desktop_test:
    name: Desktop Tests
    needs: changes
    # Desktop-specific jobs are final-audit work; keep these jobs for main PRs
    # and explicit dispatches only.
    if: >-
      needs.changes.outputs.desktop == 'true' && (
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.base.ref == 'main' &&
       !startsWith(github.event.pull_request.head.ref, 'release-please--branches--main')))
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf
      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.0
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Cache Rust build artifacts
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
            apps/desktop/src-tauri/target
          key: >-
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-${{ hashFiles('Cargo.toml', 'Cargo.lock', 'apps/desktop/src-tauri/Cargo.toml', 'third_party/glib-0.18.5/Cargo.lock') }}-${{ hashFiles('apps/desktop/src-tauri/Cargo.lock') }}-${{ hashFiles('third_party/glib-0.18.5/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-
      # Repository type-check/build is owned by Code Foundry Validation / CI,
      # which already runs the root Python, Rust, and web checks on the same
      # main-targeting PR SHA. This job keeps only desktop tests.
      - name: Test desktop
        run: bun run desktop:test

  desktop_clippy:
    name: Desktop Clippy
    needs: changes
    # Desktop-specific jobs are final-audit work; keep these jobs for main PRs
    # and explicit dispatches only.
    if: >-
      needs.changes.outputs.desktop == 'true' && (
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.base.ref == 'main' &&
       !startsWith(github.event.pull_request.head.ref, 'release-please--branches--main')))
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf
      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.0
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Cache Rust build artifacts
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
            apps/desktop/src-tauri/target
          key: >-
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-${{ hashFiles('Cargo.toml', 'Cargo.lock', 'apps/desktop/src-tauri/Cargo.toml', 'third_party/glib-0.18.5/Cargo.lock') }}-${{ hashFiles('apps/desktop/src-tauri/Cargo.lock') }}-${{ hashFiles('third_party/glib-0.18.5/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-
      # Repository type-check/build is owned by Code Foundry Validation / CI,
      # which already runs the root Python, Rust, and web checks on the same
      # main-targeting PR SHA. This job keeps only desktop clippy.
      - name: Lint desktop
        run: bun run --cwd apps/desktop clippy

  release:
    name: Release Compilation
    needs: changes
    # Desktop-specific jobs are final-audit work. Ordinary main code PRs
    # receive the complete final audit; manual dispatch remains available.
    if: >-
      needs.changes.outputs.desktop == 'true' && (
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.base.ref == 'main' &&
       !startsWith(github.event.pull_request.head.ref, 'release-please--branches--main')))
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf
      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.0
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Cache Rust build artifacts
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
            apps/desktop/src-tauri/target
          key: >-
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-${{ hashFiles('Cargo.toml', 'Cargo.lock', 'apps/desktop/src-tauri/Cargo.toml', 'third_party/glib-0.18.5/Cargo.lock') }}-${{ hashFiles('apps/desktop/src-tauri/Cargo.lock') }}-${{ hashFiles('third_party/glib-0.18.5/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-ubuntu-24.04-${{ env.CORTANA_DESKTOP_TARGET }}-desktop-rust-v2-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config*', 'apps/desktop/src-tauri/.cargo/config*') }}-
      # Linux release compilation is reserved for ordinary main code PRs and
      # explicit manual final audits. It is skipped for Release Please
      # version-only PRs.
      - name: Compile release desktop
        # Final gate: run for ordinary main code PRs and manual dispatch, not
        # Release Please version-only PRs.
        if: >-
          github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'pull_request' &&
           github.event.pull_request.base.ref == 'main' &&
           !startsWith(github.event.pull_request.head.ref, 'release-please--branches--main'))
        run: bun run desktop:build

  # Fast aggregate carrying the stable required check name. It always runs
  # after its dependencies so the check turns red on failure or cancellation
  # instead of silently skipping. Skipped dependencies (Release Please
  # version-only PRs) are acceptable and leave the check green.
  aggregate:
    name: Tauri 2 / Linux
    needs:
      [changes, gtk_provenance, gtk_iterator, security_audit, desktop_test, desktop_clippy, release]
    if: ${{ !cancelled() && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Fail if any desktop job failed or was cancelled
        if: >-
          needs.changes.result == 'failure' || needs.changes.result == 'cancelled' ||
          needs.gtk_provenance.result == 'failure' || needs.gtk_provenance.result == 'cancelled' ||
          needs.gtk_iterator.result == 'failure' || needs.gtk_iterator.result == 'cancelled' ||
          needs.security_audit.result == 'failure' || needs.security_audit.result == 'cancelled' ||
          needs.desktop_test.result == 'failure' || needs.desktop_test.result == 'cancelled' ||
          needs.desktop_clippy.result == 'failure' || needs.desktop_clippy.result == 'cancelled' ||
          needs.release.result == 'failure' || needs.release.result == 'cancelled'
        run: exit 1
      - name: All desktop jobs passed or were skipped
        run: echo "security audit, GTK provenance, GTK iterator, desktop tests, clippy, and release compilation passed or were skipped"
